import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import AppLoader from './preloader';
import Preloader from './preloader';

ReactDOM.render(
  <React.StrictMode>
    <Preloaderoader />
  </React.StrictMode>,
);
